%td
  - if option.owned_by?(current_user)
    .option.button
      = "#{option.text}&nbsp;(#{option.votes.count})".html_safe
  - else
    .option.button
      =link_to(option.text, "/options/#{option.id}/votes")

